# add-agent-system

LangGraph multi-agent orchestrator with router and six sub-agents (SQL, RAG, multimodal RAG, forecast, anomaly, viz). Graphiti temporal memory on Neo4j. Ollama default with cloud LLM behind a flag.
