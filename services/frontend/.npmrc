# Next.js prerenders its built-in /404 and /500 via the pages runtime,
# which breaks under pnpm's default isolated node_modules (duplicate
# React → "useRef of null"). A hoisted layout (npm-flat-like) is the
# documented, supported setup for Next + pnpm.
node-linker=hoisted
