// Phase 3 fills this with the chat surface (agent proxy + Vega charts).
export default function ChatPage() {
  return (
    <section>
      <h1 className="text-xl font-semibold">Chat</h1>
      <p className="mt-2 text-sm text-[var(--muted)]">
        The operator chat surface (backed by the agent service) lands in phase 3.
      </p>
    </section>
  );
}
